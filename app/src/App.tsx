/**
 * Phase 0 placeholder. PLAN.md §11 Phase 3 replaces this with the trip list and
 * the merged timeline, including the offline read cache.
 */
export function App() {
  return (
    <main>
      <h1>Trips</h1>
      <p>Scaffold only — see PLAN.md §11.</p>
    </main>
  );
}
